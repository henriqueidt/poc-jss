import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { PrimaryNav, NavItem } from 'Components';
import storybookContent from 'Config/storybookContent.json';

export const params = {
  itemClassname: 'primary-nav__item',
  linkClassname: 'primary-nav__link'
};

<Meta title="Components/01-Navigation/Primary Nav" component={PrimaryNav} />

# PrimaryNav
This is the default example for the PrimaryNav component.

<Preview>
  <Story name="Example">
    <PrimaryNav params={params}>
      {storybookContent.navItems.map(navItems => <NavItem key={navItems.link.id} fields={navItems} />)}
    </PrimaryNav>
  </Story>
</Preview>

## Props
<Props of={PrimaryNav} />
