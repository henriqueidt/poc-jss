import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { FooterNav, NavItem } from 'Components';
import storybookContent from 'Config/storybookContent.json';

export const params = {
  itemClassname: 'footer-nav__item',
  linkClassname: 'footer-nav__link'
};

<Meta title="Components/01-Navigation/Footer Nav" component={FooterNav} />

# FooterNav
This is the default example for the FooterNav component.

<Preview>
  <Story name="Example">
    <FooterNav params={params}>
      {storybookContent.navItems.map(navItems => <NavItem key={navItems.link.id} fields={navItems} />)}
    </FooterNav>
  </Story>
</Preview>

## Props
<Props of={FooterNav} />
