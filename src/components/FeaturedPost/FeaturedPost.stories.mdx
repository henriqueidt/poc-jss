import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { default as FeaturedPost } from './index.js';
export const fields = {
    heading: {
        value: 'Sample Heading'
    }
};

<Meta title="Components/FeaturedPost" component={FeaturedPost} />

# FeaturedPost
This is the default example for the FeaturedPost component.

<Preview>
    <Story name="Example">
        <FeaturedPost fields={fields} />
    </Story>
</Preview>

## Props
<Props of={FeaturedPost} />
