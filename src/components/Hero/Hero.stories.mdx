import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { default as Hero } from './index.js';
export const fields = {
    heading: {
        value: 'Sample Heading'
    }
};

<Meta title="Components/Hero" component={Hero} />

# Hero
This is the default example for the Hero component.

<Preview>
    <Story name="Example">
        <Hero fields={fields} />
    </Story>
</Preview>

## Props
<Props of={Hero} />
